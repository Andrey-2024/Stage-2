# Программа для проверки и обновления статуса заметки

# Возможные статусы заметки
STATUSES = ["выполнено", "в процессе", "отложено"]

# Шаг 1: Запрос текущего статуса заметки у пользователя
current_status = input("Введите текущий статус заметки (выполнено/в процессе/отложено): ").strip()

# Проверка корректности текущего статуса
if current_status not in STATUSES:
    print("Некорректный статус. Устанавливаем статус по умолчанию: 'в процессе'.")
    current_status = "в процессе"

# Отображение текущего статуса
print(f"\nТекущий статус заметки: \"{current_status}\"")

# Шаг 2: Предложение изменить статус
print("\nВыберите новый статус заметки:")
for i, status in enumerate(STATUSES, start=1):
    print(f"{i}. {status}")

# Шаг 3: Получение корректного выбора пользователя
while True:
    user_input = input("\nВаш выбор (введите номер): ").strip()
    try:
        choice = int(user_input)
        if 1 <= choice <= len(STATUSES):
            break
        else:
            print("Ошибка: выберите номер из предложенного списка.")
    except ValueError:
        print("Ошибка: введите число, соответствующее номеру статуса.")

# Шаг 4: Обновление статуса заметки
new_status = STATUSES[choice - 1]
current_status = new_status

# Шаг 5: Вывод обновлённого статуса
print(f"\nСтатус заметки успешно обновлён на: \"{current_status}\"")
